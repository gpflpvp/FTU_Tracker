<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FTU_Tracker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FTU_Tracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d"
    Title="FTU_Tracker">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <controls:SettingsCard x:Name="settingsCard"
                               Description="{x:Bind Viewmodel.CountDescription, Mode=OneWay}"
                               Header="This is the Header">
            <Button Style="{StaticResource AccentButtonStyle}" Content="Increment by 1"
                    Command="{x:Bind Viewmodel.CountDescriptionChangedCommand}"/>
        </controls:SettingsCard>
    </Grid>
</Window>
