<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FTU_Tracker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FTU_Tracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d"
    Title="FTU_Tracker">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

   
<!--
    <Grid>
        <controls:SettingsCard x:Name="settingsCard"
                               Description="{x:Bind Viewmodel.CountDescription, Mode=OneWay}"
                               Header="This is the Header">
            <Button Style="{StaticResource AccentButtonStyle}" Content="Increment by 1"
                    Command="{x:Bind Viewmodel.CountDescriptionChangedCommand}"/>
        </controls:SettingsCard>
     </Grid>
-->

        <Grid x:Name="RootGrid">
            
            
            <Grid.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.ThemeDictionaries>
                        <ResourceDictionary x:Key="Default">
                            <SolidColorBrush x:Key="BackgroundBrush" Color="#EEE" />
                            <SolidColorBrush x:Key="TextBrush" Color="#333" />
                        <!--      <x:String x:Key="ThemeString">Light theme</x:String>
                        <ImageSource x:Key="ImageSource">ms-appx:///Assets/SampleMedia/LightThemeSampe.jpg</ImageSource> -->
                        </ResourceDictionary>
                    <ResourceDictionary x:Key="Dark">
                            <SolidColorBrush x:Key="BackgroundBrush" Color="#333" />
                            <SolidColorBrush x:Key="TextBrush" Color="#EEE" />
                        <!--     <x:String x:Key="ThemeString">Dark theme</x:String>
                       <ImageSource x:Key="ImageSource">ms-appx:///Assets/SampleMedia/DarkThemeSample.jpg</ImageSource>  -->
                        </ResourceDictionary>
                    </ResourceDictionary.ThemeDictionaries>
                </ResourceDictionary>
            </Grid.Resources>
            <StackPanel
        MaxWidth="700"
        Padding="8"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Background="{ThemeResource BackgroundBrush}">
            <!--        <TextBlock
            Foreground="{ThemeResource TextBrush}"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Text="{ThemeResource ThemeString}" />
                <Image Source="{ThemeResource ImageSource}" />  -->
            <Button Style="{StaticResource AccentButtonStyle}" Click="btn_theme_switcher" Content="Accent style button" />
        </StackPanel>
            
            
        </Grid>




  
</Window>
